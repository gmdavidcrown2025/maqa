[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "maqa"
version = "0.1.0"
description = "MAQA broker ranking library."
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.5,<3",
]

