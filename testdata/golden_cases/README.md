# Golden Cases

该目录用于存放跨语言共享的黄金样例。

设计目标：

- 同一份输入可以被 Python 和 Go 版本共同消费
- 用于验证排序顺序和关键分值是否一致
- 后续可以逐步增加更多场景，例如：
  - 正常排序
  - 月度落后补量
  - 短期突刺惩罚
  - 超额尾流
  - 无可排序 Broker

推荐单个样例文件结构：

- `config`: 当前测试使用的参数
- `context`: 时间上下文
- `lead`: 当前 lead
- `brokers`: 候选 broker 列表
- `expected`: 期望排序结果与关键分值
